name: Build Anchor image

on:
  push:
    branches:
      - main
    paths:
      - 'apps/anchor/**'
      - '.github/workflows/anchor-image.yml'

concurrency:
  group: anchor-image
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker builder
        uses: useblacksmith/setup-docker-builder@v1

      - name: Set image tag
        id: image
        run: echo "short-sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build and push Anchor image
        uses: useblacksmith/build-push-action@v2
        with:
          context: .
          file: apps/anchor/Dockerfile
          push: true
          tags: |
            ghcr.io/novarumsocial/anchor:${{ steps.image.outputs.short-sha }}
            ghcr.io/novarumsocial/anchor:latest
